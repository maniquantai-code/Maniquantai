# ManiQuantAI

Recovered source from the production Vercel deployment.

## Development

```bash
npm install
npm run dev
```

The app expects the required Supabase and backend environment variables in Vercel/local development.

## Avatar behavior

The dashboard profile avatar uses the first character of the authenticated user's email.
For example, `abhi15724@gmail.com` displays `A`.
